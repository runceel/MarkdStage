## Native card inside a list

- ```adaptive-card json
  {
    "type": "AdaptiveCard",
    "version": "1.6",
    "body": [
      { "type": "TextBlock", "text": "Unsupported schema in a list" }
    ]
  }
  ```
